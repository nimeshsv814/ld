{{- if .Values.storageClass.enabled }}
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{ .Values.database.storageClassName }}
provisioner: nfs.csi.k8s.io
parameters:
  server: {{ .Values.database.nfs.server }}
  share: {{ .Values.database.nfs.path }}
{{- end }}