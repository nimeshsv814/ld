{{- if .Values.storageClass.enabled }}
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{ .Values.storageClass.name }}
provisioner: nfs.csi.k8s.io
parameters:
  server: {{ .Values.storageClass.nfs.server }}
  share: {{ .Values.storageClass.nfs.path }}