apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: training-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/nimeshsv814/ld
    targetRevision: main
    path: helm/services/training-service
    helm:
      releaseName: training-service
      values: values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true